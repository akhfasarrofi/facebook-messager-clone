language: node_js
node_js:
  - 12.19.0

cache:
  - npm

install:
  - npm install

script:
  - npm test
  - npm run build

deploy:
  provider: netlify
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master

