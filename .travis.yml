language: node_js

node_js:
  - 12.19.0

cache:
  - npm
  
script:
  - npm install
  - npm run build

deploy:
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master